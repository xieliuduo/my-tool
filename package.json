{
  "name": "my-tool-browser-extension",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check background.js && node --check app.js && node --check src/utils/date-time.js && node --check src/utils/text-diff.js"
  }
}
