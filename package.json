{
  "name": "my-tool-browser-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check popup.js && node --check src/utils/date-time.js"
  }
}
